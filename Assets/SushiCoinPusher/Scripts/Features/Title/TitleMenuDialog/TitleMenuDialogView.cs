using UnityEngine;
using UnityEngine.UI;

namespace SushiCatcher.Title.TitleMenuDialog
{
    public class TitleMenuDialogView : MonoBehaviour
    {
        enum ButtonType
        {
            Game,
            Achievement,
            Collection,
        }
        
        [SerializeField]
        Button[] _buttons;
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
